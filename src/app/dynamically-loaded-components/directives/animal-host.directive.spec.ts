import { AnimalHostDirective } from './animal-host.directive';

describe('AnimalHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimalHostDirective();
    expect(directive).toBeTruthy();
  });
});
